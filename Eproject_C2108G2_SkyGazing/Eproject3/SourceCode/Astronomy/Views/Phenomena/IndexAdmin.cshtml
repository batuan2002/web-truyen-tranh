@model IEnumerable<Astronomy.Models.Phenomena>

@{
    ViewBag.Title = "IndexAdmin";
    Layout = "~/Views/Shared/_layoutAdmin.cshtml";
}

<h2>List of Phenomenas</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PhenomenaName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PostDate)
        </th>
        <th>
            Poster
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr class="hover">
        <td>
            @Html.DisplayFor(modelItem => item.PhenomenaName)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PostDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PhenomenaID }) |
            @Html.ActionLink("Details", "DetailsAdmin", new { id=item.PhenomenaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PhenomenaID })
        </td>
    </tr>
}

</table>
